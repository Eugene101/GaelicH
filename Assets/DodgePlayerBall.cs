using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DodgePlayerBall : MonoBehaviour
{
    public bool isActive = true;
    // Start is called before the first frame update
}
