using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Gametype : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print(MainMenu.competitions);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
